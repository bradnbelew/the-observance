# V5 placement registry. Start empty: production commands persist only audited coordinates.
# Never paste guessed coordinates here. `/obs placehold build`, `/obs unlit site`,
# `/obs townsfolk spawn`, and `/obs wren spawn` write their exact sites atomically.
default-world: "world"

defaults:
  radius: 6
  vertical-radius: 4
  protect: true

sites: {}
