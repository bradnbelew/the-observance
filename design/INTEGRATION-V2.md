# The Observance — INTEGRATION-V2 (per-idea verdict + exact threading + build tasks)

> **The authority the Lore + Build phases follow.** One section per accepted idea: final verdict,
> exact threading (canon FACTs to add — using the FROZEN namespace from `WEB-MASTER §0`, never the
> idea files' self-assigned integers), lore/NPC/cipher/seed touchpoints, and concrete build tasks.
> Where this file and an idea file disagree, **this file wins.** Cross-references: `WEB-MASTER.md`
> (the web), `COHERENCE-AUDIT-V2.md` (contradictions/orphans), `BUILD-MANIFEST.md` (file partition).
>
> **Global preconditions (must land BEFORE the threads that read them — coherence ROOT-A/C):**
> 1. **Namespace freeze applied** (`WEB-MASTER §0`): new FACTs 16/10b/2b/7b/13b; INV 11–17; the two
>    "sixes"; the shared-anchor owners. No idea file self-assigns a FACT/INV integer.
> 2. **`iss_caught` flag reconciled** (coherence P2-3/F5): pick `iss_caught` as the single canonical
>    post-catch flag; retire `_known`/`_found` drift; document its ~six readers. **Precondition for M4.**
> 3. **Drip-ordering guard** (coherence P2-7 / prior P0-7): the drip pool never opens with a `dead_end`
>    and respects outcome semantics. **Precondition for difficulty staging + the prophet wall.**
> 4. **One M5 ending composer** exists (`WEB-MASTER §5`) before any fork/Seventh/fate/record/difficulty
>    M5 clause is authored. **Precondition for every ending-colorant idea.**
> 5. **The single Iss IV→V chain** is documented in `cipher-web` (catch → bound word → coop gate →
>    coordinate → walk → Accepting) before `coords`/`coop-gate`/`seventh` re-carve `stone_iss`.
>
> **Backlog-batch preconditions (coherence Batch-2 — apply with the above):**
> 6. **Backlog namespace freeze applied** (`WEB-MASTER §0.2`): INV-18 (single-arbiter slot), INV-19
>    (Accepting active-only quorum), INV-20 (UnlockBeat step↔payload); **FACT 17** (the word-axis). No
>    `backlog-*` file self-mints an INV name/number or FACT integer.
> 7. **The `requires_flags` activation lane** (`puzzles.requires_flags jsonb`) lands and lights the nine
>    staged back-half rows for ANY `outcome_type` (not just `dead_end`); `no-wall-catch` is repointed to
>    set `iss_caught` and STOP (no `next_puzzle_key: rite-tokens`); seedcheck adds "no `active=true` row
>    reachable only through a staged predecessor." **Precondition for the entire back half (arg-craft F1).**
> 8. **One `readActiveRoster(windowMs)`** owner (`backlog-full-showrunner`); the Accepting bridge (INV-19),
>    the spawn-bias conductor, and the keeper-NPC framework consume it. **Precondition for the climax + all
>    ambient apparitions.**
> 9. **The single-arbiter `apparitionClaim`** (INV-18): every emitting lane defers. **Precondition for any
>    second apparition/whisper lane (keeper-NPC, the Ear) beyond spawn-bias/offline/name-where.**
> 10. **`RoomSwapBeat`** (clear-A-then-paste-B, idempotent on a `swapped` PDC marker) is the ONE A→B
>    overwrite path; the `world_paste_ledger` governs single-paste set-pieces only. **Precondition for the
>    M-III Undercroft swap.**
> 11. **One `private_message` key-resolver in `resolve.ts`** (NOT `PrivateMessageBeat.java`); `voice.ts`
>    stays the single source of truth. **Precondition for the Iss cold-flip line.**
>
> **Two-folded-ideas preconditions (frame-progression must-resolve — apply with the above):**
> 12. **Ledger freeze applied** (`WEB-MASTER §9`): cursed-map = rows 24–27, progression = 28–31; no idea file
>    self-numbers a ledger integer (S1, the only P0 of the two-idea fold). **Precondition for A14/A15.**
> 13. **FACT 11 source clause sealed** in `canon-spine` + `structures.md` (*"the kept fire was carried up from
>    below the bottom… one direction, not two"*) BEFORE any Nether build (S11). **Precondition for A15's Nether
>    lane.**
> 14. **`WEB-MASTER §8` ratified** before `FateInput.netherForgeFound` is wired into `decideFate` (S9), with the
>    `kept`-fires-without-it self-test. `seventh_seen_out` is never a fate input (S2). **Precondition for the
>    Nether `kept`-lean.**

---

## A. ARC-SPINE (P1) — the threads that make the full arc worth filming

### A1 — The Seventh as a Real Optional Spine (`the-seventh-spine`)
**Verdict: KEEP-SCALED.** P1 = the choice + the ending tint (scaled form, R6); P2 = chambers 2–3 full
vertical. Gates nothing (by law).
**Canon:** ADD **FACT 10b** (sealed child of FACT 10 — *the land refused a keeper who broke nothing*).
TOUCHES 8, 15.
**Threading:**
- **Anchor ownership (WEB-MASTER §0.4):** the **dead-shrine** (`the_cold_hearth` surface = Iss's grave,
  `coords` false walk) and the **seventh-shrine** (the hearth-*deep* + `the_unwriting`) are **distinct
  places**, temporally layered: the deep opens only post-`iss_caught` + `seventh_named`. **Merge the
  `divergent-fates` `INHERITORS`/`dark_shrine` deposit INTO this shrine** — it is the same `restore`/
  deposit act; ONE flag `seventh_choice`, the `INHERITORS` codicil reads it (no separate `dark_shrine`
  site, no separate `ending_codicil` flag origin).
- **Rail-fence home (WEB-MASTER §2):** chamber 2 (the unwriting, rails=6) **reuses Brann's taught
  rail-fence literacy** — it does NOT teach the cipher cold. Gate chamber 2 on Brann's rail-fence node.
- **The one Iss/Seventh ambiguity line** is behind `iss_caught` only; canon stays "distinct."
- **The retreating glimpse is un-targeted** (the principled precision exception, INV documented).
**Lore:** D11 unchanged (already the spine); NEW `the-fire-they-let-out.md` (D-new, the cause-fragment,
correlates with D11 → FACT 10b). Run both through de-slop.
**Build tasks:** seed `seventh-unwriting` (rail-fence → `seventh_named`), `seventh-cause` (correlation
lore), `seventh-choice` (staged sentinel → `seventh_choice`); `SeventhChoiceListener` (rite-detect
sibling, `seventh_named`-gated, idempotent, one-site break-whitelist at `the_unwriting`); 3 `.schem`
assets; `sites.yml` add `the_unwriting`; voice keys `keeperCloseSeventhRestored/Erased` **feed the M5
composer** (do not write M5 directly). Self-tests: rail-fence round-trip, site-coverage, sentinel charset.

### A2 — Divergent Fates (`divergent-fates-endings`)
**Verdict: KEEP-SCALED** to 3 resolvable (`KEPT`/`CAST_OUT`/`DIVIDED`) + 1 secret (`REFUSERS`) + 1
codicil (`INHERITORS`).
**Canon:** the self-assigned "FACT 16" is **DEMOTED** to a child of FACT 10 (mechanical expression of
"the land can refuse" — `WEB-MASTER §0.1`). ADD **INV-11** (active-only enum selector, bond excluded).
TOUCHES 6, 8, 10, 14, 15.
**Threading:** the selector is the input to the **single M5 composer** (`WEB-MASTER §5`), not its own M5
writer. `INHERITORS` deposit = the Seventh `restore`/deposit act (merged, A1). `DIVIDED` splits the floor
**by geometry, never by player** (INV-16). Bond tally **excluded** from inputs.
**Build tasks:** `fate.ts` pure module + `fate.selftest.ts`; sentinel branch in `resolve.ts` (set-once
`arc_state.ending_fate` + `ending_codicil`); plugin fate-dressing beat (marker facing / floor light /
toast); `REFUSERS` detected on showrunner cadence (positive defiance, never absence). Migration:
`ending_fate text`, `ending_codicil boolean`. Voice fns feed the composer. Dashboard fate-preview
(author-only) + manual override through the approval gate.

### A3 — Record Writes You In + Counting Base-Journal (UNIFIED Hold-Book) (`record-writes-you-in` + `counting-base-journal`)
**Verdict: KEEP-SCALED, UNIFIED.** One book, one anchor (`WEB-MASTER §4`, coherence P0-3). The down-count
is the M1–M3 face; the keeper-record is the M1→M5 face. Slice = Hold-Book + M3 two-keeper column move
(Vaun/hoard, Sella/wander). P1 = six first-person hands + M5 rewrite. P2 = Iss page keyed to the reader's
own name.
**Canon:** closes **LORE-BIBLE TODO-3** (FACT 9's document home). Bodies FACT 12/14, feeds 15. INV note:
*keeper-enrolment tally is a neutral colorant; never elects, never gates.* The base-docket binding note
goes under FACT 1.
**Threading:** the down-count mirrors R10's open-markers column, **never a wall-clock** (Source A muster
or Source B Movements-to-M5 fallback). The M4 re-read is gated on `iss_caught`. **De-slop (slop A3): cut
the chiasmus** — the re-read line is *"the muster is read. the count was never of the dark. it was of the
hands. the hands are almost in."* Keeper pages obey the grammatical fingerprints (slop §C / WEB-MASTER §6).
Precision floor: a flat player is enrolled to **no one**.
**Build tasks:** `keeper-record.ts` (pure `decideKeeperEnrolment`) + `.run.ts` + selftest; `readSignal
Projection()` in repo; `LecternFillBeat` re-fire (longer `pages[]`, out-of-LoS, idempotent high-water);
`count_source` payload field resolved from the live muster (`snapshot.ts`); seed `base-docket-reread`
(M4 lore, gated behind the Vigenère node); migration `keeper_record jsonb` on `showrunner_state`; voice
`keeperPage*`, `docketReread`, `docketEven`. **Gate:** ship only the static M1 plant until the count is
bound to real state.

### A4 — Some Laws Are Lies (the forged eighth) (`some-laws-are-lies`)
**Verdict: KEEP-SCALED.** Forged *document* + cipher + paired rehearsal-for-Iss. **CUT** the inert eighth
*custom key* / null-enforcement flag. Cut the "or worse" inverted toll by default.
**Canon:** ADD **FACT 7b** (child of FACT 7 — *a later keeper forged an eighth observance; the land never
enforced it*). ADD **INV-17** (engine enforces exactly seven CUSTOM_KEYS + the one group latch; anything
else is fiction). **This resolves P0-2 with the Unlit Deep** (`WEB-MASTER §3.3`): the Covering is a forged
law about a *count*; the Unlit Deep is a real *group latch* — different frames, no collision.
**Threading:** uses the **founders' ring (six marks)** order, NOT fall-order (the two sixes, §0.3).
**De-slop (slop B4): the forged line must NOT credit "me"** — *"the founders set the ways and did not
finish the count. the eighth is the covering of the hands… cover, and be counted clean."* (anonymous lie
is the stronger lie).
**Build tasks:** NEW `the-eighth-way.md` (forged ordinance, substitution signature → "cover/hide one's
own"); margin edit to `no-wall-was-ever-built-here.md`; **no listener, no custom key**; one substitution
puzzle row (`teaches_custom = NULL`, `thread_key = 'surface'`); Aro Set-A parrot line; voice keys
`cardEighthForged`, `archiveEighthCorrection`.

### A5 — The Collective-Restraint Custom / the Unlit Deep (`collective-restraint-custom`)
**Verdict: KEEP-SCALED.** Build ONE instance (the Unlit Deep), M3-gated. **CUT** the name-restraint
instance.
**Canon:** ADD `the_unlit_deep` to `CUSTOM_KEYS` (the closed set — the **one** group latch INV-17 allows).
Touches FACT 7/8/11/12. Sub-fact under 5/9 (the group's collective conduct rhymes). Obeys INV-8 + honorless-
relief. **Coexists with the forged eighth law** per `WEB-MASTER §3.3` — never the same "eighth" frame.
**Threading:** braids deep-line + dark-hours + kept-light (all tracked). Reward = the never-doused fire
**lends its glow**; break = it withdraws for all (reversible, active-only, `broken_by` recorded-not-spoken).
Detect on **explicit flame acts only** (no ambient light sampling — precision). Cipher: polybius (the dark
25th cell) + a1z26 cross-check. **De-slop (slop A4): objects do not "remember/want"** — keep lines flat.
**Build tasks:** `UnlitDeepListener.java` (BlockPlace + debounced held-flame edge); `TrackerConfig`
`CUSTOM_UNLIT_DEEP` + config; NEW `group_restraint_state` table (window_id, state, broken_by author-only,
broke_at); `canon.ts` add the key (forces the namespace guard to thread it); voice `tollUnlitDeep`,
`keptUnlitDeep`, `CUSTOM_PHRASES.the_unlit_deep`; R01 eighth-line; Brann margin; Wenna folk-charm; Aro lie;
M4 passive-voice report. **Gate behind the Undercroft fire (FACT 11) existing.** If the active-only +
reversible + unspoken-`broken_by` mitigations can't be honored, **cut the whole idea.**

### A6 — Forced Simultaneous Cross-Surface Co-op Gate (`cross-surface-coop-gate`)
**Verdict: KEEP-SCALED.** One gate, the IV→V hinge. **3 distinct acts, not 3 distinct people** (active-
only). Do not make it the climax; no 4th/5th role; no second such gate.
**Canon:** the proposed "three hands" fact is recorded as **texture under the timeline / three-registers
note**, not a new numbered FACT (it is a gate, not a discoverable claim). TOUCHES Iss (§4), the sealed spine.
**Threading (coherence P1-1 — the single Iss chain):** the Vigenère catch yields the **bound word**; the
coop gate uses it; clearing the gate opens the Threshold whose carving yields the **true coordinate** (then
`coords` walks it). The gate does NOT also yield the coordinate — they are sequenced, not overloaded on
`stone_iss`. **De-slop (slop A4): "the threshold remembers three" → "the count is three. the threshold is
open."** (objects don't remember).
**Build tasks:** `CoopPlateListener.java` (plate-held window, writes world legs, posts opaque conjunction
token); the AND-join lives **once** in `resolve.ts` (`applyOutcome` of the gate puzzle); `coop_gate_legs`
table; `arc_state.threshold_open`; `sites.yml` add `coop_plate` (Undercroft); seed `m4-three-hands` +
two bound-word in-roads; voice `oracleThreeHands`; Mara M1 fragment + arming report.

### A7 — Coordinates to a Real Place (`coords-to-real-place`)
**Verdict: KEEP-SCALED.** Exactly TWO expedition coords (Iss false + true); the Seventh coord is P2-optional.
**CUT** the IRL/GPS reading entirely (server world only).
**Canon:** adds no fact. ADD **INV-14 (INV-COORD)** to canon §6 — *coordinate = navigation pointer only;
the answer is a clean destination word.* **Governs the grave and name-carve too** (P1-6): both must obey it
(they do — annotated below).
**Threading (the single Iss chain):** the **true** coordinate is what the **Threshold carving** yields
*after* the coop gate (not the Vigenère catch directly). The false walk = the dead-shrine (`the_cold_hearth`
surface). The destination **word** (not the coordinate) is the answer, gated to on-site presence.
**Build tasks:** `SignWriteBeat` re-carve `stone_iss` on the catch; `LecternFillBeat` leaves at each
destination; `PrivateSound/ParticleBeat` per-presence arrival; pre-pasted tableaux; two `puzzles` rows
(answers = destination WORDS, never coords); set `puzzle-key` on the destination answer-sign site (on-site
gate); voice `voice.dest.*`.

### A8 — Your Name Where You Have Never Been (`name-where-never-been`)
**Verdict: KEEP-SCALED.** One carve per movement (≈3–4), staged re-read.
**Canon:** ADD **FACT 16** (child of FACT 1 — *the record files the living by place, not only by name*).
TOUCHES 1, 9, 15. **Obeys INV-14** (the `coordEncode` back-pointer is a pointer, not an answer). **Obeys
INV-16** (subjects ROTATE across all active players — a chorus, never the divergence extremes).
**Threading:** ACTIVE-only (never offline — the separation law vs `offline-skin`, INV-16/P2-5). Cipher:
shared rune alphabet (reuse), optional atbash mirror at the M3 precise hit (Sella), `coordEncode` back-
pointer to the M1 teaching stone. **De-slop (slop B1): cap the dramatic comma-fragment** ("against each
name, a ground" once, not as a signature).
**Build tasks:** `SignWriteBeat` reused as-is; selector helper `name-where-never-been.ts` in the showrunner;
**THE ONE CODE PREREQUISITE — `player_visited_cells` per-player coarse visited-set** (table + `tracker.
visitedCells().mark()` from `LocationSampler`); selection = group-avoided cell ∩ T-never-visited; sites
`carve_anchor` candidates; one Archivist place-filing fragment; voice clueDrip key; Keeper half-veiled M4
line. **P-gate:** the beat does not fire without positive proof-of-absence for T.

### A9 — The Future-Dated Grave (`future-dated-grave`)
**Verdict: KEEP-SCALED** (one grave, not a graveyard). P0 single-grave slice cut available.
**Canon:** ADD **FACT 13b** (child of FACT 13 — *the stone is cut before the keeper is kept*). TOUCHES
12, 14, 15; rhymes with FACT 10's Seventh grave (one promised, one refused). **Obeys INV-14** (the date is
*read*, never typed as an answer).
**Threading:** the carved date == the **single Accepting instant** (coherence P1-8 — bound in `arc_state`,
shared with the Record website timestamp and the summons `not_before`; NO idea owns the instant, the
showrunner does). Rune + digit glyphs (both literacies). **De-slop (slop A1): the private receipt must not
warm or self-justify** — *"the one called {name}. read first. cut first. the rest are not yet cut. they
will be."* (cut "you read it first. so it is your name first.").
**Build tasks:** **wire `not_before<=now` into `fetchActionableBeats`** (or the showrunner flip) + selftest
FIRST; `showrunner/grave.ts` producer (grounded active name, date=Accepting, two-row transaction A+B
idempotent, one grave); `grave_state` row; reuse `the_threshold` (or `grave_spur` off-spur); voice
`graveCarved/graveOpened/graveReceipt`; founder margin doc; Discord rune-card + Keeper second-door node.

### A10 — Dynamic Diegetic Difficulty (`dynamic-diegetic-difficulty`)
**Verdict: KEEP, HARD-SCALED.** Keep cadence multiplier + dead-end staging + register selection + the
plant/payoff. **CUT the Whisper-budget knob entirely** (it never reads/writes `whisper_budgets`).
**Canon:** ADD **FACT 2b** (child of FACT 2 — *the land's grip is not fixed*). ADD **INV-15** (difficulty
never touches the Whisper backstop). TOUCHES 9, 10.
**Threading (coherence P2-7):** dead-end staging requires the **drip-ordering guard** (precondition #3) —
the difficulty engine must NOT stage a dead-end into the opening drip slot. Group-scalar only (no per-player
rank). The M5 backward-read feeds the **composer** (Mara's "closer count" re-quote), not a standalone line.
**De-slop (slop A2): the planted fragment must NOT name "mercy" or resolve its own meaning** — *"i called
that a cruelty for a winter. i do not call it that now, and i will not say what i call it."*
**Build tasks:** `reckoning.ts` pure module + selftest (injected constants); `types.ts` add `ReckoningState`
/`Tone`; `decide.ts` multiplies `dripIntervalMs` (one line, pure); `snapshot.ts` distinct-solver/first-try/
whisper-lean; `state.ts` `reckoning_state`/`reckoning_since_ms` (hysteresis); `apply.ts` flips staged
`dead_end` `active` (Discord-side, no MC write); voice `tone` arg + `deepTightens`/`deepIsPatient`; seed
one Mara bookCipher lore row + `min_state` tags; dashboard health-panel state label. **`grep` the module
for `whisper_budgets`; a hit is a bug.**

### A11 — Mutually-Exclusive Forks With Permanence (`exclusive-forks-permanence`)
**Verdict: KEEP-SCALED.** Fork A (P0), Fork B (P1), Fork C (P2/cuttable on blurt).
**Canon:** ADD **INV-12** (permanence colors, never gates; felt at the ending, never scored; subtracts only
paired with a new artifact). TOUCHES 5/Vaun, 6, 7/8, 10, 11.
**Threading (coherence P1-7, INV-13):** **the fork-arming Sacred Beast is the one GLOWING animal; the herd-
conversion Pale never glow** — so the irreversible fork is always fairly avoidable. Forks feed the **M5
composer** (color, never gate; seedcheck: no spine puzzle requires a fork flag). **De-slop (slop B2): the
kept-light leaf must not reassure** — *"the light came up the stair on its own. you carried it. that is how
it is carried."* (cut "you only", "that is enough", "meant to be carried").
**Build tasks:** extend `DeathListener` (last-tagged-glowing-beast kill → `sacred_beast_broken`, first-
writer-wins); `SacredAnimalBeat` mark the last tagged beast as fork-arming; seed `fork-light`(B),
`fork-name`(C) (`set_flags` + side-thread clue); `arc_state.flags` for all leaves; `sites.yml` Undercroft
doused variant; Vaun conditional node, Mara `light_taken` re-read; voice keys for each leaf line (feed the
composer); **seedcheck assertion: no spine puzzle requires a fork flag.**

### A12 — The Slow Herd Conversion (`herd-conversion`)
**Verdict: KEEP-SCALED.** Cosmetic, capped, between-session spread. **CUT** any version where the Pale are
tracked, glow, breed, or convert on-screen.
**Canon:** adds no fact. ADD **INV-13 (INV-HERD)** (merged with the fork rule — only the glowing Beast is
tracked; cosmetic Pale never glow; the fork-arming Beast is the glowing one). Touches 11, 12, 15.
**Threading:** Discord NEVER announces "the herd grows" (no step-ladder). Marquee leans on **orientation**
(formation), not number. Cipher: prior-keeper bookCipher count fragment (Mara); optional P2 a1z26 prediction
stone.
**Build tasks:** `SacredAnimalBeat` `mode:"spread"` + `target` + `pale_cosmetic` PDC (distinct from
`sacred_beast`, never glowing, capped 16, one-per-pass, unwitnessed, babies not auto-pale); `DeathListener`
`paleCosmeticKey` ignored for conduct (the precision guard); `paleTarget` deterministic lookup in
`snapshot.ts`; collective-gaze facing pass (the one new code dep for the marquee); `sites.yml` `herd_anchor`;
two found-docs (bookCipher fragment + margin note). Reuse `tollSacredBeast`/`keptSacredBeast` (no new key).

### A13 — The ARG Leaves the Game (`arg-leaves-the-game`)
**Verdict: KEEP-SCALED.** Record website (P1) + stego visual-rune-layer (P1). **CUT LSB stego** (re-encode
fragility). **CUT the real astronomical new moon** → encoded timestamp (P2). **CUT** the live-mutating page
(static-per-build only) and any personalization not backed by a measured signal.
**Canon:** adds no fact (third door for FACT 1/8/11/14/15). The "record kept in more than one place" stays
**texture**, not a numbered fact.
**Threading (coherence P1-8):** the encoded timestamp == the **single Accepting instant** (shared with the
grave + the summons `not_before`). The Iss entry re-renders cold on `iss_caught`. Reads the **spoiler-free
projection only** (never sealed rows). Stego carries exactly the Iss Vigenère key (P17→P3), a second door.
**Build tasks:** `/record/[slug]/page.tsx` (server component, noindex, no client JS); `record-projection.ts`
(pure, spoiler-free-gated); forge `embedRuneLayer` + round-trip self-test (must not break the **X1 plaintext
round-trip guard** — see specscheck flag below); `clue-drip` stego branch; founder-line plant; seed
`record-url` (P4 lore, gates nothing); voice `recordElsewhere`, `recordReceives`.

### A14 — The Cursed Adventure-Map Frame (the Prologue / on-ramp) (`cursed-map-frame`)
**Verdict: KEEP — P0 on-ramp + cold-open, scaled.** The delivery vehicle for the Cold-Start Prologue's
ignition for a **remote/async** group (the one thing the in-server prologue can't do), and the YouTube
cold-open. **Mints NOTHING** — no FACT, no INV, no cipher, no flag, no site; a new *surface* (the lure page),
a *prop* (the vignette), and ONE voice key. **Extends A13** (the Record website): same route, same projection,
same `the-record-keeps` slug, same M5 `recordReceives()` fill. **Path A holds** — the vignette is an optional
client-side PROLOGUE PROP, never the server engine's delivery vehicle.
**Canon:** adds no FACT, no INV (confirmed against `WEB-MASTER §0.1/§0.2` — nothing to mint). Touches FACT
1/2 (the page IS `/record`; the vignette rehearses `the-record-opens`), FACT 10/10b (the doused hearth +
scraped wall = the Seventh's *unnamed* texture), FACT 14/15 (the `6` → six prior groups kept; the group is the
seventh, kept as hands), and the `record-writes-you-in` thesis (the dead uploader = a dead hand still keeping).
**Threading (the synthesis resolutions, frame-progression must-resolve):**
- **THE LEDGER FREEZE (S1, the one P0):** the cursed-map's plants are **rows 24–27** (`WEB-MASTER §9`),
  synthesis-assigned — NOT the idea file's self-numbered "next free row #24+". The idea file must stop
  self-numbering; this file + `WEB-MASTER §9` own the integers.
- **FB-2(i) CUT (S5, precision):** the map-conduct callback ("you passed the seventh in the hold *too*… it was
  noted *there*") is **cut** — the offline single-player vignette never transmits its local log to the server,
  so asserting a map-side fact the server cannot have measured is an INV-16 / §6.4 precision lie. **The
  GROUP-FACING count-callback FB-2(ii) is the ONLY frame-break body** (*"six were kept before you… hands are
  kept"* — true for everyone, names no one, a thing the static map could not say). Default-safe: no clean
  signal → fire only `recordOpened()`.
- **ROUTE RE-SCOPE (S4):** `record/[slug]/page.tsx` is **slug-AGNOSTIC today** (`grep slug` → 0 — no `params`
  arg, no slug branch; it is NOT "reads `v_record` without validating the slug"). The build is therefore: add
  `{ params }`, render the BASE archive for bare `/record` + the existing slug, the **downloads block ONLY for
  `the-record-keeps`**, and an in-voice 404 for any other slug — preserving `noindex`, no client JS, the
  `v_record`-only read.
- **`from_map` distinction (S7):** the bare `#the-record` `messageCreate` detector **cannot** tell a
  map-arrival from a cold join (both just post). So **default: the frame-break (count-callback) fires for
  everyone** (safe — the `6` is true regardless of whether they played the map). The keener `from_map` flag
  (set by an arrival beat) is **P2/optional**, not assumed.
- **Seal the `6`→six-keepers equivalence (S6):** the `6` is inert/ambiguous on the page (a dead file's tally);
  the M2 "soft" read is "six downloads, six keepers" as *pattern-match*, NOT "the keepers were download groups
  like us" (which is the M4 felt landing). LORE confirms it doesn't over-blurt FACT 15 on day zero.
- **One Mara author (S3):** the lure-page provenance line (`m.kept`) is authored by LORE in the **same pass**
  as `minecraft-progression`'s D-NETHER-1 (both new Mara surfaces), against `journals-vaun-mara-sella.md` — she
  cites/defers, never *acts*; Mara-who-copies-and-keeps and Mara-who-reads-but-won't-walk are the same hand.
**Lore:** the lure-page provenance line (Mara's deferred hand, signed `m.kept`); the README "lie-that-is-true"
("it does not connect to anything"); the vignette closing-page pointer; the count-callback frame-break text.
OPTIONAL `[GAP — cuttable] the-copy-i-kept.md` (a four-line Mara margin the page can quote instead of citing
`kept-in-more-than-one-place` inline).
**Build tasks:** `voice.recordFrameBreak()` (ONE key, count-callback default body — add to `OracleVoiceKey`
union only if seeded via a row); slug-validate + downloads block on `record/[slug]/page.tsx` (static `kept: 6`
+ one recovered-file entry + struck-7); the vignette asset `public/the-hold/` (vanilla structures +
command-blocks + ONE datapack function — no engine, no Supabase, no keeper name past the Archivist, no cipher
key, no server flag; scaled 1-room → full 6-room hold); arrival wiring (route the vignette hand-off into the
BUILT `messageCreate` ignition detector; emit `recordFrameBreak()` once on the measured signal via the BUILT
ack path, idempotent on the `acked` guard). **Tables/flags: NONE new** (reuses `prologue_ignited` + the
`acked` one-shot). **Gates NOTHING** (the campaign completes with the map, lure page, and frame-break entirely
absent).

### A15 — Vanilla Progression as Canon (Nether = the deep fire-source; End = exile / the Seventh's absence) (`minecraft-progression`)
**Verdict: KEEP-SCALED, CRITIQUE-RESOLVED.** Two **OPTIONAL DEEPENING LANES** off the Overworld spine: the
Nether (FACT 11's source made walkable — the Kept-Light origin + "you are becoming them" embodied) deepens
M2→M3; the End (the one place outside the record — the Seventh's exile + the `cast_out`/`refusers` fates'
place) deepens M4→V. **Both gate nothing** (INV-12, INV-19); a portal-skipping group gets a whole un-shaded
arc. Anchors are **ratified canon** (`WEB-MASTER §0.4`: `nether_forge`, `end_seventh_shrine`, `end_exile_hold`,
`observance_nether`, `observance_end`, `seventh_seen_out`), not self-minted.
**Canon:** adds **NO new sealed FACT** (both lanes are *delivery bodies*) and **no new INV** (namespace is
synthesis-owned). TOUCHES FACT 11 (Nether = source, "below the below"), FACT 12 (soul sand = the worst keeping,
*deep-time*), FACT 10/10b (the End = where the refused went), FACT 14/15 (felt, never stated). Governed by
INV-11/12/14/16/19.
**Threading (the synthesis resolutions):**
- **THE LEDGER FREEZE (S1):** progression's plants are **rows 28–31** (`WEB-MASTER §9`), synthesis-assigned —
  NOT the idea file's self-numbered "next free row #24+". Status column normalized to `NEW`. The idea file
  stops self-numbering.
- **FACT 11 SEAL IS PENDING-IN-CANON (S11, P1 BLOCKER):** `[S3-RESOLVED]` in the idea file is
  resolved-in-DESIGN, not in-canon — `canon-spine` FACT 11 still has no source clause. **LORE seals ONE
  sentence into `canon-spine` FACT 11 + `structures.md` BEFORE any Nether build:** *"the kept fire was carried
  up from below the bottom; the Undercroft is the bottom of the Hold, the deep-fire its source — one
  direction, not two."* Until sealed, the journals say "below the below," never "the real bottom," and the
  lane does not ship. **No second "real bottom"** (R1).
- **`netherForgeFound` SELECTOR EDIT IS PENDING, NOT SCHEDULED (S9):** adding `FateInput.netherForgeFound` to
  `decideFate` is a frozen-interface edit to `WEB-MASTER §5/§8`. It is **BLOCKED on synthesis ratifying §8**,
  with the self-test **`kept` fires fully without it**. Until ratified, the row sets `nether_forge_found` and
  the M5 composer reads it for a tint, but `decideFate` does NOT. `seventh_seen_out` is **NOT** a fate input
  (S2) — it licenses an already-measured `cast_out`/`refusers` fate to *read back* at the End place; it touches
  the M5 composer only.
- **INV-16 TEETH ON THE END-CITY `cast_out` PLACE (S10, P2/cuttable):** the exile-hold names **no living
  player and encodes no per-player side** — chorus-only dressing ("*you only came to look*"), never the
  `LEFT_AT` set, no dressing spatially corresponding to any per-player carve. **If the open End can't guarantee
  that, the binding is cut and the End ships as the Seventh shrine ALONE** (the default).
- **REVEAL-SAFETY (S6):** the End shrine re-dresses an *already-generated* end-ship (force-load → mutate on
  unwitnessed relog → unload) or a *world-build pre-generated* island — never a lazy paste toward an
  approaching glider. The End has **ZERO ambient apparition lane** (R3, a positive canon choice).
- **PRODUCER BOUNDARY (S7):** re-dressing *occupied* vanilla structures is **additive pastes onto
  verified-clear adjacent air** (`SmallStructureBeat` on a clear sub-region) or `RevealBeat` flips — **never**
  an occupied overwrite, **never** `RoomSwapBeat` (Undercroft-only); bound to the `world_paste_ledger`
  single-paste owner (`WEB-MASTER §0.5`).
- **WALK BUDGET (S4):** the Nether P1 core is a **NEAR POCKET**, not a third decoded coordinate-trek (budget =
  2 ground walks + ≤1 short vertical pocket; INV-14: the on-site word answers, never the coordinate). The full
  bearing-trek is P2 behind a playtest.
- **soul sand ≠ the Pale herd (S5):** soul sand = *deep-time, before-the-Kept* failed communities (the journal
  anchors "older than the first keeper"); the Pale herd = *this* group's present-tense conversion. The two
  FACT-12 visuals never read as the same population.
- **One Mara author (S3):** D-NETHER-1 is authored with the cursed-map's `m.kept` line in one LORE pass.
**Lore:** D-NETHER-1 `the-fire-is-lent.md` (Mara, bearing-page, `clue_bearing:true`, M3); D-NETHER-2
`the-fire-kept-me.md` (the pocket keeper, decaying hand, `clue_bearing:false`, M3-4); D-END-1
`the-name-i-cut-myself.md` (the Seventh, from outside the record, M4). TOUCH `the-seventh-not-kept.md` (D11 —
no edit; already the End lane's plant). The unwriting wall's extra effaced line (a reveal on the existing
surface, no new row).
**Build tasks:** `sites.yml` add `nether_forge` (`world: observance_nether`), `end_seventh_shrine`
(`world: observance_end`), `end_exile_hold` (`world: observance_end`, `enabled:false` until INV-16 binding
built); two Multiverse worlds (the Undercroft pattern); seed `nether-forge` (`lore`, sets `nether_forge_found`
+ Whisper, `requires_flags:[undercroft_open]`) + `end-seventh-out` (`lore`, sets `seventh_seen_out`,
`requires_flags:[seventh_named]`); breadth rows `dest-deep-forge`/`dest-out-of-record`; `arc_state` flags
`nether_forge_found` (the proposed `FateInput.netherForgeFound`, S1-ratified before it feeds `decideFate`) +
`seventh_seen_out` (M5-composer + `seventh_choice`-context only, S2); voice keys `nether.soulSand`,
`nether.forgeArrive`, `end.shrineArrive`, `end.outsideRecord`, and the M5 composer clauses
`fateCastOutEndRead`/`fateRefusersEndRead` (replace the neutral fate clause, ≤2-clause cap holds); reuse
`SmallStructureBeat` (additive-on-air), `RevealBeat`, `LecternFillBeat`, `SignWriteBeat`, `AnswerSignListener`
+ `Site.contains` (the dimension-presence signal — `LocationSampler` already keys on `worldName`, zero new
infra); extend `siteCoverageSelfTest` to assert the named world exists before a cross-dimension row activates
(R7). **No new tracker/listener/cipher; no `RoomSwapBeat`; the website is NOT widened** (R9/S8). **Gates
NOTHING** (INV-12); **not in the Accepting quorum** (INV-19); **not P0** (the slice proves the Overworld loop).

---

## B. DEPTH (P2) — high-payoff, low-cost, builds after the slice + spine

### B1 — The Day-One Meta-Puzzle / `UNKEPT` (`day-one-meta-puzzle`)
**Verdict: KEEP-SCALED.** One meta-word (`UNKEPT`), one terminal lore node, zero new engine/cipher. The
naive "first letter of each plaintext" form is **CUT** (X1 guard); the acrostic lives in the carved
**framing**, never the bound run.
**Canon:** adds no sealed fact; strengthens 6/10/12. Non-sealed anti-orphan note: *the six maker's marks,
read in **fall-order**, carry `UNKEPT`; planted M1, legible M4; gates nothing.* **Uses fall-order, NOT ring-
order** (the two sixes, §0.3 — the order-key clue names fall-order; glyphs fail in ring-order, self-correcting).
**Threading (coherence P2-4):** the six maker's-mark glyphs are part of **one coordinated M1 teaching-stone
layout** authored by synthesis alongside the prologue marker and the `kept here before you` plant, so each is
individually legible. Nods at the smaller `D04` acrostic (deliberate escalation, P2-1).
**Build tasks:** seed `meta-unkept` (lore, `['unkept']`, movement 4, `active:false`); `clue-specs.ts`
`NON_CIPHER_KEYS` entry (keeps `specsCoverageSelfTest` green; X1/CLUE_SPECS untouched); voice `oracleMetaUnkept`;
six per-stone framing edits in `cipher-plaintexts.md` (free prose, X1-safe).

### B2 — Dead-Ends With Teeth (`dead-ends-with-teeth`)
**Verdict: KEEP-SCALED.** Taunt family (P1, cheapest win). Prophet's wall = **Iss content, not a new liar**
(P2). **CUT** any taunt that gloats/threatens/names a feeling.
**Canon:** adds no fact; texture on 2, 5, 7/8, 10.
**Threading (coherence P2-1/P2-7):** the prophet's wall is **wide, not tall** (independent `dead_end` rows,
no `next_puzzle_key` — not a countable ladder). Requires the **drip-ordering guard** (precondition #3) so a
staged dead-end never opens the drip. The hidden columnar name = Iss; re-reads at `iss_caught`. **De-slop
(slop §A doctrine): the taunt states the category of truth + its inertness, nothing else.**
**Build tasks:** extend `oracleDeadEnd` to a `kind`-switched family (recommend the **single-fn `kind` arg**,
lowest blast radius — pass `voice_args.kind` through the existing `resolve.ts` case); re-point the 3 seeded
dead-ends to `kind`s; (P2) `prophet-wall-comfort` (substitution), `prophet-wall-name` (columnar) rows + one
margin line in `the-ways-are-a-wall.md`. No new beat/listener/site.

### B3 — The Apparition Wearing an Offline Player's Skin (`offline-skin-apparition`)
**Verdict: KEEP-SCALED.** Rare reflection/edge/crouch re-skin. **CUT** walking/following/pathfinding; **CUT**
name-tag on every firing (allow exactly ONE human-approved named M4 beat); **CUT** chat from it.
**Canon:** adds no fact (carrier of FACT 9). Touches 12, 15.
**Threading (coherence P2-5, INV-16):** skin-wearing is **OFFLINE-only**; never co-located with a
`name-where-never-been` name-carve at the same keeper stone/window. Worn skin = the offline player
**canonically rhymed** to the shape (collective, never a callout). Despawns reveal-disciplined if the worn
player rejoins. Silhouette fallback if the skin won't load.
**Build tasks:** `NamedMobBeat` `skin_player` + `offline_only` payload + `applyWornSkin` (cache-first,
silhouette fallback) + re-check at fire; `PresenceListener.onJoin` → `despawnApparitionsWearing(uuid)`;
per-worn-player one-shot budget; M1 offline-player report (plant); voice key for the M4 FACT-9 line +
optional whisper deferral.

### B4 — The Cold-Start Prologue (`cold-start-prologue`)
**Verdict: KEEP-SCALED** to "re-stage the existing first report + one physical seed." **CUT** any bespoke
set-piece/unique mob/special broadcast. **NOTE: this is the one P0 vertical-slice ignition idea** (it is
authored as depth-priority in its own file but its *priority is P0* — without ignition, nothing else gets
tested).
**Canon:** adds no fact (delivery of FACT 1/2; touches 14). Log the lit-marker cross-surface seed in the
LORE-BIBLE FACT-web audit (second door onto FACT 2/3).
**Threading (coherence P2-4/P2-6):** the prologue marker glyph is part of the **one coordinated M1 teaching-
stone layout** (with the UNKEPT glyphs + the `kept here before you` plant). D01 plant additions are
**budgeted by synthesis** — the prologue inherits D01 wholesale; it does NOT add a buried tease (the
place-elsewhere line goes to the Record website, the muster/count form to the Hold-Book; per P2-6 the others
find a second M1 doc home, not all on D01).
**Build tasks:** `arc_state.flags.prologue_ignited` + snapshot field; `IgnitionListener` (lectern read OR
`#the-record` human post); `decide.ts` guard (no curatorial drip before ignition; gifts still apply); gated
one-shot `recordOpened` ack; `sites.yml` add `first_marker_01` + retarget `first_report_lectern_01` to base
hot-cell; conditional named report (overwhelming single signal) else un-named FACT-1 fallback; add the
marker glyph to the Rosetta taught set (M2 payoff).

---

## C. INTEGRATION-WIDE BUILD GUARD FLAGS (must be honored — see BUILD-MANIFEST)

- **X1 plaintext round-trip guard** (`specsSelfTest`): the `UNKEPT` acrostic (B1) and the stego layer (A13)
  must NOT touch any bound cipher plaintext. Both live in framing/visual layers; verify `specscheck` stays
  green after each.
- **Cross-surface register normalizer** (`registerDisciplineSelfTest`): every new Watcher/keeper voice key
  (Hold-Book pages, fate lines, grave lines, Seventh closes, difficulty tones, dead-end taunts, coop ack,
  Record lines) must pass lowercase/no-caps/no-exclaim/no-meta-word. All S1/S2 slop fixes are applied at
  authoring (see each idea's de-slop note above).
- **`customKeyNamespaceSelfTest`**: adding `the_unlit_deep` (A5) to `CUSTOM_KEYS` forces it into both
  `TrackerConfig.java` and `voice.ts` or the build fails — this mechanically enforces the threading.
- **`threadTagSelfTest` / `threadCardVoiceCoverageSelfTest`**: every new puzzle row + Recovery-Archive card
  (forged-eighth surface card, fate card, three-hands card, record-url) must carry a valid `thread_key` and a
  defined `body_voice_key`.
- **seedcheck assertion (NEW, A11)**: no spine puzzle's availability `requires` a fork flag.
- **noLeakedSentinelSelfTest**: the `seventh-choice`, `m4-three-hands`, and Accepting sentinels carry opaque
  plugin-posted answers — none may describe itself as a placeholder.
- **`activationReachabilitySelfTest` (NEW, backlog F1)**: every `requires_flags`/staged row is named by one
  activation rule; no `active=true` row is reachable only through a staged predecessor.
- **`unlockStepContractSelfTest` (NEW, INV-20)**: every `UnlockBeat` `step` names a registered beat with a
  contract-matching payload.

---

## D. THE PRIOR-SESSION BACKLOG (the load-bearing engine the new ideas hang on)

> These ten `backlog-*` reconciliation specs are the spine the Section-A/B threads ride. Each was authored
> faithful to the built code but re-opened a shared engine seam; the verdict + threading below applies the
> Batch-2 resolutions (`WEB-MASTER §0.5`, coherence Batch-2). **None is cut**; scope is reconciled.

### D1 — The Between-Session Showrunner (`backlog-full-showrunner`)
**Verdict: KEEP — the keystone.** The single highest-leverage missing piece; everything async hangs on it.
**Canon:** adds no fact. **Owns `readActiveRoster(windowMs)`** (the single active-set source, §0.5) and the
**M5 ending composer** skeleton (§0.4 precondition #4).
**Threading:** authors the daily clue drip, personalized reports (scalpel + **deterministic fallback** via
`voice.reportObserved/Escalated`), stone-difficulty/Whisper tuning, the AUTO↔CONFIRM split (player-earned
`approved`; curatorial `pending`). Its `liar.ts` producer is **demoted** from the Iss activation lane to
**optional curated re-staging** of Iss's warm beats as cold (D5). Every output has an in-engine deterministic
fallback (the SPOF mitigation).
**Build tasks:** `run.ts` authoring loop (drip, gifts, reports, grave/herd/keeper passes); `readActiveRoster`;
`liar.ts` (re-staging only); the composer skeleton; deterministic-fallback wiring on every personalized call.

### D2 — The Six Keeper-Stone Expeditions (`backlog-keeper-stone-expeditions`)
**Verdict: KEEP — the M2 load-bearing field.** Six rows, six ciphers, six verbs, six keeper voices.
**Canon:** adds no fact; embodies FACT 5 (the rhyming chorus). Brann's stone is rail-fence's **single teaching
home** (night-gated, fire-count rail key — un-orphans rail-fence; `WEB-MASTER §2`).
**Threading:** the new-ARG M2 plants (grave, forged eighth, first name-carve, herd) hang on this field. The
`UNKEPT` meta-acrostic uses the six maker's-mark headers in **fall-order** (NOT "Rosetta sunwise" = ring-order
— coherence Batch-2 P2-5; one-line fix in the expeditions file §5).
**Build tasks:** six `puzzles` row-sets (caesar/book/atbash/vigenère/substitution/coord, each a different
answer-verb); six FAWE set-pieces + forged clues + `UnlockBeat` reveals; the six maker's-mark framing glyphs.

### D3 — The Two Rosettas (Reckoning + Rune-Ring) (`backlog-keeper-stone-expeditions` + cipher-web §4)
**Verdict: KEEP.** The rune-ring (`D03`, master literacy) + the **Stone of Reckoning** (digit-glyphs, `-`/`,`
marks) make substitution and `coordEncode` fair in-world.
**Canon:** adds no fact; sets `rosetta_known` / `reckoning_known` (soft staging flags).
**Threading (arg-craft F1 literacy fix):** the rune-ring is NOT the sole literacy door — the **`a1z26`
teaching-rung** (a number puzzle, no runes) is the genuinely-different second in-road (`WEB-MASTER §2`).
**Build tasks:** two FAWE/inline Rosetta set-pieces + `SignWriteBeat`/`LecternFillBeat`; the `a1z26` rung row.

### D4 — The Liar Engine (`backlog-liar-engine`)
**Verdict: KEEP — and it owns the activation lane.** The one-key-two-doors re-walk.
**Canon:** adds no fact (mechanizes §4 the Liar thread). **Owns `puzzles.requires_flags`** (§0.5) — the
deterministic gate that lights the whole back half (arg-craft F1). Its `private_message` fix lands in
`resolve.ts` (§0.5), not `PrivateMessageBeat.java`.
**Threading:** warm reading → `iss-dead-shrine` (`dead_end`); name-as-key → `iss_caught` (opens `bound-word`).
`no-wall-catch` sets `iss_caught` and **stops** (no rite shortcut). The Vigenère stego key (A13) and a
`stone-orin` stego layer are the **two real in-roads** to the bound word (arg-craft F2).
**Build tasks:** `requires_flags jsonb` column + `getOpenPuzzles` filter + `repo.ts` join; `resolve.ts`
`private_message` key-resolver; repoint `no-wall-catch`; seed the two bound-word in-roads.

### D5 — The Undercroft Dimension + A→B Swap (`backlog-undercroft-dimension`)
**Verdict: KEEP.** Multiverse void world (ambient-light-0, datapack `dimension_type` fog) + the M-III
"room rebuilds itself" swap.
**Canon:** delivers FACT 11 (the one fire that never went out). **Owns `RoomSwapBeat`** (§0.5) — the ONE A→B
overwrite path (the FAWE file's paste-over claim is **cut**, coherence Batch-2 P0-3).
**Threading:** two descent in-roads (Mara book-cipher `undercroft-descent` AND a Brann night-beacon / Seventh
Whisper path — arg-craft F2). The Unlit Deep (A5) and the First Light fork (A11) live here.
**Build tasks:** Multiverse world + fog datapack; `RoomSwapBeat extends SmallStructureBeat` (clear-A-then-
paste-B in one `mutateWhenUnwitnessed`, idempotent on a `swapped` PDC marker, `require_floor:false`).

### D6 — UnlockBeat Producers (`backlog-unlockbeat-producers`)
**Verdict: KEEP.** Author `outcome_payload.beat = {type:"unlock", payload:{step:...}}` rows so solved clues
visibly open the world; no Java change to the dispatcher.
**Canon:** adds no fact. ADD **INV-20** (step↔payload contract). Declares the producer boundary (§0.5): a NEW
`RevealBeat` (block-state/slot-lighting, no FAWE) vs `SmallStructureBeat` (paste) vs `RoomSwapBeat` (swap).
**Threading:** the M5 rite ships as a `reveal` slot-lighting interim until FAWE lands (compatible with D5).
**Build tasks:** `RevealBeat.java`; `UnlockBeat` producer rows across the seed; `unlockStepContractSelfTest`.

### D7 — Bestiary Spawn-Bias (`backlog-bestiary-spawn-bias`)
**Verdict: KEEP — and it owns the apparition slot.** Showrunner logic, not new beats; ships all six creatures
on vanilla fallbacks first.
**Canon:** adds no fact. ADD **INV-18** (single-arbiter `apparitionClaim`, §0.2). Probabilistic + capped
(never a deterministic callout — the precision contract).
**Threading:** the conductor reads `SignalSnapshot` + `readActiveRoster`, runs `selectApparition`, publishes
`apparitionClaim`; offline-skin, name-where, keeper-NPC, and the Ear all defer (INV-18, P1-4).
**Build tasks:** the conductor pass (probabilistic, capped, `apparitionClaim` publish); reads
`SignalSnapshot.java`; emits built `NamedMobBeat`/`SacredAnimalBeat`/`Private*` beats.

### D8 — Keeper-NPC Framework (`backlog-keeper-npc-framework`)
**Verdict: KEEP.** Citizens2 presiding Keeper + ZNPCsPlus per-player prior-keeper apparitions; M-IV atonement
gating. Closes **FACT 9 / LORE-BIBLE TODO-3** by the dialogue route.
**Canon:** adds no fact (FACT 9 carrier). Its apparitions **defer to `apparitionClaim`** (INV-18). FACT-9
delivery is **one surface per player per window** (coherence Batch-2 P1-5).
**Threading:** `keeper.ts` (dialogue resolver) ≠ `keeper-record.ts` (Hold-Book producer); `keeper.ts` is NOT
`reckoning.ts`. The Iss node-text cold-swap reads the same `iss_caught`.
**Build tasks:** `KeeperNpcBeat.java` (`NPCRightClickEvent` → JSON dialogue tree reading the dossier);
`keeper.ts` + `keeper.run.ts`; M-IV atonement node; the `fact9.named` line (de-slopped — see slop E3/E4).

### D9 — The Accepting Sentinel Bridge (`backlog-accepting-sentinel-bridge`)
**Verdict: KEEP.** The plugin posts the opaque normalized sentinel to the shared resolver only after the
custom listeners confirm simultaneous-bow + deposits + window — so the terminal `main_beat` inherits the
`solves` replay-guard for free.
**Canon:** ADD **INV-19** (active-only quorum, folds `INV-ACC-ACTIVE`). `effectiveQuorum =
min(configQuorum, activeRosterSize)`, read from `readActiveRoster` (§0.5) — never punishes an absent member.
**Threading:** gates on ACTIVE players only; defers its M5 close to the **single composer** (never writes M5).
**Build tasks:** the `min(...)` rule in `AcceptingRiteListener.java`; consume `readActiveRoster`; opaque
plugin-posted sentinel (charset-clean, `noLeakedSentinelSelfTest`).

### D10 — FAWE Large Set-Pieces (`backlog-fawe-large-setpieces`)
**Verdict: KEEP-SCALED.** The `.schem` branch in `SmallStructureBeat`'s `mutateWhenUnwitnessed` wrapper for
large set-pieces (keeper-stones, Undercroft rooms, the M-III swap source).
**Canon:** adds no fact. The `world_paste_ledger` governs **single-paste only**, keyed `(world, site_id,
schematic, base)`; the A→B swap is the `RoomSwapBeat` marker's job, NOT the ledger (§0.5; the file's
"paste-over is legal" claim is **cut**, coherence Batch-2 P0-3).
**Build tasks:** the `schematic` payload branch (footprint-pre-check on `clipboard.getDimensions()`,
`ignoreAirBlocks(true)` + `copyEntities(false)`, protect the region, tag in `world_paste_ledger`);
`world_paste_ledger` migration; the `.schem` assets (go-live).

### D11 — Modeled-Mob + Voice / The Ear (`backlog-modeled-mob-and-voice`)
**Verdict: KEEP — phased (cosmetic P2, voice P3).** `ModeledMobBeat` (ModelEngine R4, degrades to exactly
`NamedMobBeat`); the late-arc voice layer (`VoiceListener` = the Ear, `SpatialVoiceBeat`).
**Canon:** ADD **FACT 17** (the word-axis, §0.1) — assigned by synthesis, NOT self-minted. The Ear is P3;
FACT 17 plants at P2 and degrades to a pack-sound whisper if SVC never installs.
**Threading:** the Ear's `voice_watchlist` **derives from** the keeper roster + bound plaintexts (self-tested
⊆, §0.5) — never a hand-kept copy. The Ear's `SpatialVoiceBeat` **defers to `apparitionClaim`** (INV-18). The
M2 Archivist place/word fragment is **one artifact two clauses** (shared with FACT 16, §0.5).
**Build tasks:** `ModeledMobBeat.java` (rig-after-spawn, silent fallback); `VoiceListener.java` +
`SpatialVoiceBeat.java` (fall back to `PrivateSoundBeat observance:keeper_voice`); `voice_watchlist` derive +
self-test; the FACT-17 M2 plant clause (LORE, shared fragment).
