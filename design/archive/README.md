# Pre-V5 design archive

Status: superseded historical material; never production authority.

Every file and subdirectory under `design/archive/` is preserved only to explain prior decisions.
Old claims of “canonical,” “source of truth,” “build manifest,” or “runbook” inside this tree are
historical quotations. Nothing here may supply story text, accepted answers, flags, commands, setup
steps, world geometry, or launch status for V5.

Use `design/V5-SUPERSESSION-MAP.md` to locate current authority. Promoting anything from this tree
requires reconciling it to the V5 manifests, adding automated parity checks, and explicitly listing
the new authority in that map.
