# THE OBSERVANCE — MINECRAFT-INGEST-PREP

> The definitive, ordered **GO-LIVE + TESTING** checklist: cold repo → friends pre-ignited on the server.
> Regenerated by the final QA/integrator pass (2026-06-29) against the REAL working tree (not the design
> docs alone). Authority for *what each beat contains* = `WEB-MASTER.md` / `INTEGRATION-V2.md`; authority
> for *what is owed* = the `PLAYTHROUGH-SCRIPT.md` **GAP REGISTER** (62 collected items). Where this file
> and an older doc disagree, **this file is the post-build reconciliation** — it was written after verifying
> the tree, and it flags the docs that have gone stale because the build moved.
>
> **Every line here is grounded.** A claim of "BUILT" means the file exists on disk (path given). A claim
> of "[GAP]" means it is genuinely absent (verified by `find`/`grep` this pass). The GAP numbers are the
> `PLAYTHROUGH-SCRIPT.md` GAP-REGISTER ids.

---

## 0. THE STATE OF THE TREE (verified 2026-06-29 — what the GAP register gets wrong)

The build has moved **ahead** of the GAP REGISTER in several places. The register is the de-dup of inline
markers authored across segment passes; some markers were written before the artifact landed. **Corrections
applied below; the GAP REGISTER should be patched to match.**

**STALE GAPs (the artifact now EXISTS — downgrade the register):**
- **GAP #30** `RoomSwapBeat extends SmallStructureBeat` — **BUILT**: `plugin/.../beats/lib/RoomSwapBeat.java`.
- **GAP #56 (partial)** the record route — **BUILT and slug-aware**: `dashboard/src/app/record/[slug]/page.tsx`
  is the slug authority (`{ the-record, the-record-keeps }` closed set, lure/base/in-voice-404 branch,
  `noindex`, `v_record`-only read, static `kept: 6` + struck-7). The CP1-7 "re-scope owed" / "ignores slug
  today" claim in `COHERENCE-AUDIT-V2` and `CURSED-MAP-SITE.md §status` is now **stale** (the code shipped).
  What REMAINS owed under #56: the live V-injection (`recordReceives` struck-row fill at the Accepting).
- The plugin **beat library is essentially complete**: `RevealBeat`, `UnlockBeat`, `KeeperNpcBeat`,
  `ModeledMobBeat`, `SpatialVoiceBeat`, `SacredAnimalBeat`, `WhisperTollBeat`, `FaweSchematicPaster` +
  the FAWE `schematic` branch inside `SmallStructureBeat`, `ResourcePackPusher`/`ResourcePackTracker` all
  exist on disk. The implied "beat owed" GAPs are mostly stale.
- `requires_flags` **is seeded** in `metapuzzle_seed.sql` / `progression_seed.sql` / `puzzles_seed.sql`.
- The showrunner autonomy passes (grave / herd / forks / clock) **are wired** in `run.ts`.

**REAL GAPs the register is right about — and the single load-bearing pattern they form:**

> **The engine can ENACT the back half but cannot yet DETECT the signals that arm it.** The beats exist;
> the **six signal listeners that set the new arc flags do not.** This is the headline structural gap.

Confirmed ABSENT this pass (`find plugin/src -iname …` → 0 hits each):
`IgnitionListener.java`, `UnlitDeepListener.java`, `SeventhChoiceListener.java`, `CoopPlateListener.java`,
`RefusalRiteListener.java`, `VoiceListener.java`. Without them: `prologue_ignited` is read but never set
(GAP #16); the Unlit Deep latch, the Seventh restore/erase choice (GAP #36 — the choice is inert), the coop
gate, the REFUSERS fate (GAP #52), and the Ear all have no signal source.

Also confirmed ABSENT (code/text owed, not stale):
- `voice.recordFrameBreak()` (GAP #15) — not in `voice.ts`.
- The **M5 composer** assembler (GAP #50) — clause TEXT lives in `fate.ts`/`forks.ts`; **no `composer.ts`**.
- `getOpenPuzzles` **`requires_flags` filter** (GAP #31) — `repo.ts:361` filters `active=true` ONLY; the
  seeded `requires_flags` is **inert until the code reads it**. `metapuzzle_seed.sql` itself documents this
  dependency. **This is the bug that keeps the whole back half dark — the single most important code GAP.**
- The three NEW **seedcheck guard self-tests** — `activationReachabilitySelfTest`, `unlockStepContractSelfTest`,
  `watchlistSubsetSelfTest` — are **specified in `BUILD-MANIFEST §8` but absent from `seedcheck.ts`** (only
  the original `accepted_answers` normalization parity assertion ships). See §GUARD-RISK below.
- `migrations/0006_*.sql` (GAP #1) — does not exist; every back-half column/table depends on it.

---

## 1. GO-LIVE CHECKLIST (ordered; the order is load-bearing)

### Stage A — Rotate secrets FIRST (a live key is committed)
- [ ] **A1.** ROTATE the Supabase `service_role` JWT + anon key for `fdnmhbpxnodrnbrzrlqq` (committed in clear
      in `dashboard/.env.local`). Confirm `dashboard/.env.local` + `discord/.env` gitignored. **[OPERATOR]**
- [ ] **A2.** Server box env `OBSERVANCE_SUPABASE_KEY=<rotated>`; leave `config.yml service-key: ""`.
- [ ] **A3.** `discord/.env` from `.env.example`: bot token, app/guild/`#the-record` ids, SUPABASE url+key.
- [ ] **A4.** Dashboard env (Vercel, not committed): NEXT_PUBLIC_SUPABASE_URL/ANON_KEY, SERVICE_ROLE_KEY,
      `ADMIN_EMAILS`.

### Stage B — SQL APPLY ORDER (the order is the dependency graph)
- [ ] **B1.** Dashboard migrations `0001 → 0002 → 0003` (record/auth schema + `v_record` read path).
- [ ] **B2.** Engine migrations `0003_discord → 0004_oracle → 0005_threads` (all present on disk).
- [ ] **B3.** **AUTHOR + apply `0006_*.sql` [GAP #1 — TO BUILD].** Must add, in one migration:
      `arc_state.ending_fate text`, `arc_state.ending_codicil bool`, `arc_state.nether_forge_found bool`,
      `arc_state.seventh_seen_out bool`, the single **Accepting-instant** column; `coop_gate_legs`;
      `group_restraint_state`; `player_visited_cells`; `keeper_record jsonb` on `showrunner_state`;
      `grave_state`; `reckoning_*`; **`puzzles.requires_flags jsonb`**; **`world_paste_ledger`**
      (UNIQUE `(world, site_id, schematic, base_x, base_y, base_z)`); **`voice_watchlist`**.
- [ ] **B4.** Engine seeds in dependency order (all idempotent, `ON CONFLICT DO NOTHING`):
      `puzzles_seed.sql → seventh_seed.sql → side_quests.sql → thread_tags.sql → thread_cards.sql`.
- [ ] **B5.** `metapuzzle_seed.sql` (UPDATE-only — writes `requires_flags` on the 9 staged back-half rows +
      repoints `no-wall-catch` to set `iss_caught` and STOP). **Apply ONLY after 0006 (`requires_flags`)
      exists.** ⚠ **Inert until the `getOpenPuzzles` filter ships (B-CODE below) — seed alone does nothing.**
- [ ] **B6.** `progression_seed.sql` last (Nether + End). The two payoff rows (`nether-forge`,
      `end-seventh-out`) stay `active=false` (STAGED) until the worlds + sites are placed (Stage D/E).
- [ ] **B7.** **SEAL the FACT-11 source-clause [GAP #2 — BLOCKER]** into `canon-spine.md` FACT 11 +
      `structures.md`: *"the kept fire was carried up from below the bottom; the Undercroft is the bottom of
      the Hold, the deep-fire its source — one direction, not two."* **Precondition before ANY Nether build.**

### Stage B-CODE — the code the seeds depend on (apply with/before the seeds go live)
- [ ] **BC1. `getOpenPuzzles` `requires_flags` filter [GAP #31 — the dark-back-half bug].** Extend
      `repo.ts:getOpenPuzzles` to join `arc_state.flags` and treat a row as open iff `active=true` AND every
      key in `requires_flags` is truthy. **Until this lands, B5 is decoration and the entire back half is
      either dark (`active=false`) or wide-open (`active=true`) — never gated.**
- [ ] **BC2. `resolve.ts` activation + sentinels [GAP #31, #36, #51].** `requires_flags` open-check in the
      match step; `private_message` key→subtitle resolver; fate sentinel (set-once `ending_fate`); Seventh
      two-token sentinel → `seventh_choice`/`ending_codicil`; coop AND-join (single writer).
- [ ] **BC3. The three NEW seedcheck guards [GUARD-RISK].** Add `activationReachabilitySelfTest`,
      `unlockStepContractSelfTest`, `watchlistSubsetSelfTest` to `seedcheck.ts`; gate the seed on them.

### Stage C — Resource pack (Path A: the friends' ONLY install)
- [ ] **C1.** `npm run pack:build` (atlas/font from `discord/src/forge/runes.ts` — the SAME source as the
      Discord cards); `npm run pack:proof`. (`ResourcePackPusher.java` is BUILT — pushes on join.)
- [ ] **C2.** Author the four `.ogg` bodies (`whisper`, `drone_low`, `stone_breath`, `cold_toll`). **[GAP #3]**
- [ ] **C3.** Zip; host public HTTPS; compute 40-char lowercase SHA-1; set `config.yml resource-pack.url/sha1`
      **[GAP #5]**; author the `prompt` line (plain keeper register) **[GAP #4]**; `required:false` → test → `true`.

### Stage D — Datapacks / mods / worlds
- [ ] **D1.** Paper 1.21.x / Java 21; drop the plugin jar; first boot writes `config.yml` + `sites.yml`.
- [ ] **D2.** Undercroft fog datapack (`dimension_type`/biome: thick fog, `ambient_light: 0`). **[GAP #6]**
- [ ] **D3.** Install Multiverse; create the three worlds: Undercroft void (+ D2 fog), `observance_nether`,
      `observance_end`. **Must exist before B6's cross-dimension rows may flip `active=true`. [GAP #7]**
- [ ] **D4.** (Optional, graceful-degrade) Citizens2 + ZNPCsPlus, ModelEngine R4, Simple Voice Chat. The
      Overworld spine runs without them (`KeeperNpcBeat` PDC armor-stand fallback; `ModeledMobBeat` →
      `NamedMobBeat`; `SpatialVoiceBeat` → `PrivateSoundBeat`).

### Stage E — Prologue vignette (the cursed-map prop + website) — OPTIONAL on-ramp, gates nothing
- [ ] **E0. Cursed-map WEBSITE deploy.** Deploy the dashboard (Vercel) — the **route is BUILT**
      (`record/[slug]/page.tsx`). Verify `/record/the-record-keeps` renders base archive + downloads block +
      static `kept: 6` + struck-7; `/record/the-record` renders base; any other slug = in-voice 404; `noindex`
      holds. **[GAP #56-remainder]** the live struck-row V-injection is added at the finale (Stage H).
- [ ] **E1.** Build the prologue vignette `the-hold.zip` (prefer a **datapack** — no browsable world). Vanilla
      structures + command-blocks + ONE datapack function carrying ONLY FACT-1/2 strings + the server address
      + the rune string. **No engine, no Supabase, no keeper name past the Archivist, no cipher key.** Spec is
      BUILT (`prologue/PROLOGUE-VIGNETTE.md`); the `.zip` asset + baked address + rendered rune string + host
      + lure `href` are **[GAP #8].** 1-room ships first; full 6-room is P1→P2.

### Stage F — Build every site in-game, export `.schem`, place (ALL `[GAP — GO-LIVE]`)
> `sites.yml` is authored (all ids present: two Rosettas, six keeper-stones, the custom/named anchors,
> `the_unwriting`, `coop_plate`, `first_marker_01`, `carve_anchor_01..03`, `herd_anchor`, `grave_spur`,
> `nether_forge`, `end_seventh_shrine`, `end_exile_hold` [disabled], `bastion_remains`) — but **every coord
> is `null`/UNPLACED [GAP #12]**, and **no `.schem` exists in the repo [GAP #9, #10, #11].** A `null`-coord or
> `enabled:false` site is **silently skipped** by the plugin — never an error at a player.
- [ ] **F1.** Build + export each Overworld `.schem`; fill `sites.yml` coords as you place each.
- [ ] **F2.** Three Seventh chambers `seventh_hearth_01/02/03.schem` **[GAP #10]**; Room-A/Room-B for the swap
      **[GAP #11]**; the carve/herd/grave anchors; `first_marker_01` + the rune-ring server icon **[GAP #14]**.
- [ ] **F3. Nether lane** (after B7 seal): place the `nether_forge` keeper-grave keeper-remains **at
      world-build** (never pasted toward a player); soul-sand deep-time dressing. Flip `nether-forge`
      `active=true`. Kept-Light origin shelf `kept_light_home_01` **[GAP — GO-LIVE].**
- [ ] **F4. End lane** (after `seventh_named` reachable): re-dress an already-generated end-ship OR a
      world-build-pregenerated island as `end_seventh_shrine` (reveal-safe, additive pastes onto verified-clear
      adjacent air ONLY — never an occupied overwrite, never `RoomSwapBeat`). Flip `end-seventh-out`
      `active=true`. `end_exile_hold` stays `enabled:false` unless the INV-16-safe chorus-only binding is built
      **[GAP #47 — P2/cuttable]**.

### Stage G — Locked server config (lives only on the box)
- [ ] **G1.** `config.yml tracker.forbidden-words` = the Unspoken word value (currently `[]`) **[GAP #13]**.
- [ ] **G2.** `rites.accepting.quorum` = cast size; unlit-deep / pale-spread / difficulty-state knobs.

### Stage H — Dashboard / Record / finale deploy
- [ ] **H1.** Deploy the dashboard director's console (approval gate, health panel showing `reckoning_state`).
- [ ] **H2.** Wire the live `/record` V-injection: `recordReceives()` fills the struck-7 row at the single
      Accepting instant **[GAP #56-remainder]**.
- [ ] **H3.** `observance:the_record_receives_you` advancement JSON (the FACT-14 hidden toast) **[GAP #55]**.
- [ ] **H4.** Render deploy: bot Background Worker + showrunner Cron.

---

## 2. MOVEMENT-BY-MOVEMENT BUILD ORDER (built vs prepared-pending-a-client)

> "Prepared-pending-a-client" = the server-side artifact is built/specced and waits ONLY on an in-game build,
> a hosted asset, a coord fill, or the operator's manual gate — **never on a friend installing anything beyond
> the one resource pack** (Path A). The downloadable vignette (M0) is the lone client-side prop and gates nothing.

### M0 — Prologue / The Cursed Map (optional on-ramp)
- **BUILT:** the record route (slug authority, lure/base/404, `noindex`, static `6`, struck-7); the lure-page
  lore (`kept-in-more-than-one-place.md`, `six-were-kept-before-you.md`, `the-copy-i-kept.md`); the vignette
  SPEC (`PROLOGUE-VIGNETTE.md`); `prologue.ts` (the ordering POLICY; reads `prologue_ignited`).
- **[GAP]:** `IgnitionListener` to SET `prologue_ignited` (#16); `voice.recordFrameBreak()` (#15); the
  `recordOpenedNamed`/`prologueNamed` key-mismatch resolver (#17); the `the-hold.zip` asset + host (#8).
- **Prepared-pending:** website deploy (E0); vignette `.zip` bake (E1).

### M1 — The Notice (Establishment)
- **BUILT:** the oracle resolver + normalizer (`resolve.ts`/`normalize.ts`); `AnswerSignListener`,
  `ChatListener`, `TerritoryListener`, `PresenceListener`; the Rosetta/teaching seeds in `puzzles_seed.sql`;
  `keeper-record.ts` (the Hold-Book M1 plant module); the a1z26 literacy rung row.
- **[GAP]:** live `keeper_record.ts` muster binding (#19, static-first); the rune-ring icon (#14); the
  doc↔seed rune reconciliation (#23).

### M2 — The Ways + Nether lane
- **BUILT:** the six-stone field seed; `reckoning.ts` (difficulty module exists); `forks.ts`, `herd.ts`,
  `grave.ts` (pure policies); `SacredAnimalBeat`, `NamedMobBeat`; `conductor.ts` (spawn-bias); `liar.ts`
  (pure liar policy); the forged-eighth + UNKEPT framing seeds; `progression_seed.sql` Nether rows.
- **[GAP]:** `SacredAnimalBeat` spread + `pale_cosmetic` PDC (#21); spawn-bias conductor + `apparitionClaim`
  wiring (#22); the showrunner drip/report loop (#18); `nether.*` voice keys (#25); the Nether `.schem`/world.
- **Prepared-pending:** FACT-11 seal (B7); Nether world + site placement (F3).

### M3 — The Undercroft + The Liar
- **BUILT:** `RoomSwapBeat.java` (#30 — STALE, now built); `SmallStructureBeat` FAWE branch; `RevealBeat`;
  `liar.ts`; the bound-word/coord seeds; the Undercroft descent seeds.
- **[GAP]:** `UnlitDeepListener` + `group_restraint_state` + `canon.ts the_unlit_deep` key (#34);
  `CoopPlateListener` (coop gate signal); `liar.run.ts` DB/clock wrapper (#32); the Whisper economy
  data-layer + `hintBody` strings (#24); the `no-wall-catch` `next_puzzle_key` monorail repoint (#48);
  `bound-word` second in-road stego layer (#33); the staged `stone-brann` railFence activation (#29).
- **Prepared-pending:** Undercroft fog datapack + void world (D2/D3); Room-A/B `.schem` (F2).

### M4 — Atonement, the Keeper + End lane
- **BUILT:** `KeeperNpcListener` + `KeeperNpcBeat` (the NPC framework); `offline-skin.ts`,
  `name-where-never-been.ts`; the End seed rows; `ModeledMobBeat`, `SpatialVoiceBeat` (the Ear beat + fallback).
- **[GAP]:** `SeventhChoiceListener` (#36 — the choice is INERT without it; the single largest Seventh gap);
  `VoiceListener` (the Ear signal source); `npcVoice.ts` registry (#28); the `keeper.*` cold-Iss/rhyme/neutral
  bodies + the two de-slop fixes (#40–#44); `eighth_seen` flag-set row (#45); the End-lane pointer reveal
  line (#46); the cause-fragment doc is BUILT (`the-fire-they-let-out.md` — #39 STALE, verify).
- **Prepared-pending:** End world re-dress + site enable (F4); `end_exile_hold` INV-16 binding (#47, P2).

### M5 — The Accepting + the five closes
- **BUILT:** `fate.ts` (`decideFate` pure, INV-11 inputs, active-only); `fate.selftest.ts`; the fork/fate
  clause TEXT in `fate.ts`/`forks.ts`; `AcceptingRiteListener` (the rite detector); the grave/herd autonomy
  passes wired in `run.ts`.
- **[GAP — the finale is the densest gap cluster]:** **the M5 COMPOSER assembler** (`composer.ts` — #50, the
  single largest finale gap; the clause text exists, the ≤2-clause+codicil assembler does not);
  `resolve.ts` fate-sentinel branch (#51); `RefusalRiteListener` (#52 — REFUSERS unreachable without it);
  `AcceptingRiteListener` `readyGate` + `readActiveRoster` wiring (#53); the Accepting-instant binding writer
  + `grave.run.ts`/`fate.run.ts` (#49); the fate floor-dressing producer (#54); the Nether-carrying V-tint key
  (#57); `FateInput.netherForgeFound` is PENDING §8 ratification, NOT wired (#58).
- **Prepared-pending:** the live `/record` struck-row fill (H2); the FACT-14 advancement (H3); Fork C (#61,
  P2/cuttable); season-2 Inheritors corpus (#62, deferred by design).

---

## 3. TESTING ORDER (prove each layer before the next)

1. **Self-tests green (CI gate).** `specs.selftest`, `stego.selftest` (X1 round-trip), `registerDiscipline`,
   `customKeyNamespace`, `threadTag`, `threadCardVoiceCoverage`, `noLeakedSentinel`, `specsCoverage`,
   `siteCoverage`, `decide.selftest`, `fate.selftest`, `scenario.selftest`, `customs.selftest`,
   `autonomy.selftest`, `seedcheck` — **plus the three NEW guards once authored (BC3).** All must pass before
   any seed is applied.
2. **Seed dry-run.** `npx tsx src/oracle/seedcheck.ts` against `puzzles_seed.sql` — every `accepted_answers`
   entry normalized + non-empty. Then the reachability/unlock/watchlist guards.
3. **Resolver round-trip.** With 0006 + BC1 live: solve a front-half clue on BOTH surfaces (in-game sign +
   Discord). Confirm a `requires_flags` back-half row stays closed until its flag is set, then opens.
4. **Activation lane.** Catch Iss → `iss_caught` set → `bound-word` opens (and ONLY then). This is the
   single test that proves the back half is neither dark nor wide-open.
5. **Apparition arbitration.** Two lanes contend → `apparitionClaim` lets exactly one fire (INV-18).
6. **Quorum.** Accepting fires on `min(configQuorum, activeRosterSize)`; an offline member never blocks it
   and is never punished (INV-19).
7. **Reveal discipline.** Grave/Seventh chambers/herd spread: confirm nothing mutates in line of sight
   (the per-player two-path reveal).
8. **M5 dry-run.** Force each of the five closes through the composer; confirm ≤2 clauses + ≤1 codicil,
   fixed priority, deterministic fallback if the model is offline.
9. **Cross-surface truth.** The same fact reads consistently on Minecraft ↔ Discord ↔ dashboard ↔ `/record`
   ↔ lore, one register apart.

---

## 4. GUARD-RISK (must hold across the seed/code changes — do not let drift fail the camera)

- **X1 plaintext round-trip** (`specs.selftest.ts`/`stego.selftest.ts`): re-run after the UNKEPT framing edits
  AND the Iss-card stego layer (#33). Both must leave bound plaintexts byte-identical. **The acrostic lives in
  carved FRAMING, never the bound run** — the single highest-risk edit for X1.
- **Cross-surface normalizer** (`registerDisciplineSelfTest`): re-run after EVERY `voice.ts` insert
  (recordFrameBreak, nether/end keys, the keeper de-slop fixes). Lowercase, no caps/exclaim/meta-word.
- **`seedcheck` normalization parity:** the ONLY shipped seedcheck assertion. The three NEW guards
  (`activationReachabilitySelfTest`, `unlockStepContractSelfTest`, `watchlistSubsetSelfTest`) are **absent**
  — until authored, the dark-back-half bug, an `UnlockBeat` step↔payload mismatch, and a `voice_watchlist`
  drift can ALL ship undetected. **Author BC3 before the back-half seeds go live.**
- **The 10 guards from the new rows** (per `BUILD-MANIFEST §8`): `customKeyNamespaceSelfTest`
  (`the_unlit_deep` must appear in `canon.ts` + `TrackerConfig.java` + `voice.ts` — currently the listener +
  config + key are GAP #34, so this guard will FAIL until they land); `threadTag`/`threadCardVoiceCoverage`
  (every new row + card — `cardNetherForge`/`cardEndSeventhOut` bodies are GAP #26, will fail until present);
  `noLeakedSentinel` (Seventh/three-hands/Accepting stay opaque); the A→B swap idempotency (assert the
  `swapped` PDC marker is the ONLY guard, never also a `world_paste_ledger` row).

---

*Regenerated 2026-06-29 by the final QA/integrator pass. Ground truth = the working tree, verified file-by-file.*
